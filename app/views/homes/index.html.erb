<% content_for :title, "Homes" %>

<div class="w-full">
  <% if notice.present? %>
    <p class="mb-5 inline-block rounded-md bg-green-50 px-3 py-2 font-medium text-green-700 dark:bg-green-950 dark:text-green-300" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Home Page</h1>
  </div>
</div>
